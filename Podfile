# Uncomment the next line to define a global platform for your project
# platform :ios, '8.3.3'

#platform :ios, '8.3.3'
use_frameworks!

target 'BookSearch' do
	pod 'SwiftyJSON'
    pod 'SDWebImage/WebP'
    pod ‘Google/SignIn’

end
